<%= form_with(model: task, local: true) do |form| %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :task_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>


